#include <stdlib.h>
#include "libft.h"

char *ft_strtrim(char const *s1, char const *set)
{
	size_t	i;
	size_t	j;
	char	k;

	i = 0;
	j = ft_strlen(set);
	while (*s1)
	{
		k = ft_strchr(set,*s1);
		if (k == 0)

	}

}
